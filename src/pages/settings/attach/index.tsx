/** @format */

import { Attach } from '@/pages/components/Attach';
import { PageContainer } from '@ant-design/pro-layout';

export default () => {
    return (
        <PageContainer>
            <Attach />
        </PageContainer>
    );
};
